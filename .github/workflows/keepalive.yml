name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # 每 10 分鐘執行一次
  workflow_dispatch:         # 可手動觸發

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render App
        run: |
          curl -s https://yilanyadorder-backend.onrender.com > /dev/null
